class Solution:
    def findCenter(self, edges: List[List[int]]) -> int:
        # The center must appear in both edges[0] and edges[1]
        a, b = edges[0]
        c, d = edges[1]
        return a if a == c or a == d else b
